import { <%= props.table.pascalCaseSingularName %>Model, I<%= props.table.pascalCaseSingularName %>ModelData } from '@/models/<%= props.table.kebabCaseSingularName %>.model';
import { convertToISODateString } from '@stokei/nestjs';
import { nanoid } from 'nanoid';

export class <%= props.table.pascalCaseSingularName %>ModelMock extends <%= props.table.pascalCaseSingularName %>Model {
  constructor(data?: Partial<I<%= props.table.pascalCaseSingularName %>ModelData>) {
    super({
      _id: nanoid(),
      name: data?.name ?? '<%= props.table.pascalCaseSingularName %> Name',
      parent: data?.parent ?? 'anyParent',
      createdAt: data?.createdAt ?? convertToISODateString(Date.now()),
      updatedAt: data?.updatedAt ?? null
    });
  }
}
