import { EventPublisher } from '@nestjs/cqrs';
import { Test } from '@nestjs/testing';
import { Remove<%= props.pascalCaseSingularName %>CommandHandler } from '.';

describe('Remove<%= props.pascalCaseSingularName %>CommandHandler', () => {
  let remove<%= props.pascalCaseSingularName %>CommandHandler: Remove<%= props.pascalCaseSingularName %>CommandHandler;

  beforeEach(async () => {
    const moduleRef = await Test.createTestingModule({
      controllers: [],
      providers: [
        Remove<%= props.pascalCaseSingularName %>CommandHandler,
        {
          provide: EventPublisher,
          useFactory: () => ({
            mergeObjectContext: jest.fn().mockImplementation(() => ({
              changedPassword: jest.fn(),
              commit: jest.fn()
            }))
          })
        }
      ]
    }).compile();

    remove<%= props.pascalCaseSingularName %>CommandHandler = moduleRef.get(Remove<%= props.pascalCaseSingularName %>CommandHandler);
  });
  afterEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(remove<%= props.pascalCaseSingularName %>CommandHandler).toBeDefined();
  });
});
